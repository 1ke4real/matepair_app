import {StyleSheet} from "react-native";

const defaultStyle = StyleSheet.create({
    container: {
        backgroundColor: '#111111',
        flex: 1,
    },
    text: {
        color: '#EEEEEE',
    }
});


export {defaultStyle};